# Pantheon dev BFF (lupin) — live mode
# Auto-falls back to mock + shows LiveBffBanner if transport fails.
VITE_BFF_MODE=live
VITE_BFF_BASE_URL=https://pantheon-lupin-dev-bff.34.81.75.241.sslip.io
# Non-secret dev browser token for the lupin dev BFF stub auth gate.
# This is only accepted when the dev BFF is explicitly deployed with
# PANTHEON_BFF_AUTH_STUB=true; staging/prod stay strict JWT/OIDC.
VITE_BFF_DEV_BEARER_TOKEN=pantheon-dev-browser:reviewer
