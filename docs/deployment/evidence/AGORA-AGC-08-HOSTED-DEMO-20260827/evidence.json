{
  "schema_version": "pantheon.task-review-evidence.v1",
  "task_id": "AGORA-AGC-08-HOSTED-DEMO-20260827",
  "owner": "Antigravity2",
  "reviewer": "Codex",
  "target_repo": "execute-plans",
  "merge_target": "dev",
  "branch": "task/AGORA-AGC-08-HOSTED-DEMO-20260827",
  "status": "ready-for-independent-exact-head-review",
  "recorded_at": "2026-08-27T21:30:00.000Z",
  "scope": {
    "adopted": [
      "Tighten docs/contracts/agora/demo-run-evidence.v1.schema.json with strict fail-closed constraints: reject zero SHAs (frontend_sha, bff_sha, manifest_pair_id), reject unknown/empty object IDs (proposal_id, persona_id, workshop_id, message_event_id, reconstruction_id, strategy_id, version_id, interaction_id), and enforce that status=passed requires viewer_write_denied=true, cross_tenant_non_enumerating=true, no_order_route_proof=true, read_only_restored=true, served_manifest_verified=true, and all steps have status=passed without skips.",
      "Update .github/workflows/agora-hosted-acceptance.yml to run fail-closed desktop-only acceptance (--project=chromium), remove mobile-chromium projects and mobile evidence, remove always-run hardcoded verified evidence, and enforce exact checkout/deployment/BFF SHA bindings.",
      "Update .github/workflows/pfg-agora-journey-e2e-hosted-acceptance.yml to require operator and viewer credentials via governed dev-login, enforce exact SHA bindings, and upload sanitized acceptance evidence.",
      "Fix e2e/agora-product-journey.spec.ts: initialize feSha, bffSha, and all entity IDs (workshopId, messageEventId, reconstructionId, strategyId, strategyVersion, proposalId, candidateDecisionId, decisionEventId, interactionId, performanceReceiptId, personaId) at the test start, validate each ID is non-empty and does not contain unknown, use initialized IDs in negative controls (viewer write denial, cross-tenant isolation, zero order route proof), strictly enforce that readOnlyRestored is true only when deploymentProfile is read-only and VITE_BFF_REAL_WRITES is false, and format demoEvidence per demo-run-evidence.v1 contract.",
      "Configure playwright.config.ts for desktop-only chromium project.",
      "Update src/test/agora-demo-run-evidence-schema.test.ts with 8 unit tests validating all positive and negative schema constraints (zero SHAs, unknown IDs, false negative controls, false restoration, and skipped steps)."
    ],
    "not_changed": [
      "Backend routing and interaction worker implementations.",
      "Read-only vs real-writes security boundary."
    ]
  },
  "validation": {
    "vitest_schema_unit": {
      "command": "npx vitest run src/test/agora-demo-run-evidence-schema.test.ts",
      "result": "passed: 8/8 tests passed (including zero SHA, unknown ID, false negative control, false restoration, and skipped step rejections)"
    },
    "vitest_deploy_safe_defaults": {
      "command": "npx vitest run src/test/deploy-safe-defaults.test.ts",
      "result": "passed: 20/20 tests passed"
    },
    "typecheck": {
      "command": "npm run typecheck",
      "result": "passed: tsc -p tsconfig.agora.json --noEmit with 0 errors"
    }
  },
  "review_binding": {
    "sha_disposition": "Independent approval must bind the final PR head that contains this implementation and evidence manifest."
  },
  "independent_review_required": [
    "Confirm docs/contracts/agora/demo-run-evidence.v1.schema.json strictly rejects zero SHAs, unknown IDs, and invalid status=passed states.",
    "Confirm .github/workflows/agora-hosted-acceptance.yml runs fail-closed desktop-only acceptance on chromium, with no mobile projects or always-run hardcoded evidence.",
    "Confirm e2e/agora-product-journey.spec.ts initializes all IDs at test start, validates observed IDs without falling back to unknown strings, and enforces strict read-only restoration checks.",
    "Confirm playwright.config.ts defines desktop-only chromium project.",
    "Confirm npm run typecheck and vitest unit tests pass cleanly."
  ]
}
